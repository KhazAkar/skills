# Shared firmware references
This folder contains reference material shared by firmware skills.
It is used by firmware-reverse-engineering and firmware-development.
Keep shared guidance here so both skills use one source of truth.
Rule: link, never copy.
